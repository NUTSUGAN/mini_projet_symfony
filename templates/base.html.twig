<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}IPSSI Blog{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">

    {% block stylesheets %}{% endblock %}
</head>

<body class="d-flex flex-column min-vh-100">
    {% include 'partial/nav.html.twig' %}

    <main class="flex-grow-1">
        <div class="container py-4">
            {% include 'partial/flash.html.twig' %}
            {% block body %}{% endblock %}
        </div>
    </main>

    <footer class="footer mt-auto py-3 border-top bg-white">
        <div class="container d-flex justify-content-between align-items-center">
            <span class="text-muted">© {{ "now"|date("Y") }} IPSSI Blog</span>
            <span class="text-muted small">Symfony + Bootstrap</span>
        </div>
    </footer>

    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ asset('js/app.js') }}"></script>

    {% block javascripts %}{% endblock %}
</body>
</html>
